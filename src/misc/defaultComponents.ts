import { IScannerComponents } from '../types';

export const defaultComponents: IScannerComponents = {
    audio: true,
    tracker: undefined,
    onOff: false,
    finder: true,
    torch: true,
    zoom: false
};
