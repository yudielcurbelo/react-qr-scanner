import IScannerComponents from '../types/IScannerComponents';

export const defaultComponents: IScannerComponents = {
    audio: true,
    torch: true,
    count: false,
    tracker: false,
    onOff: false
};
